/*
//vector<int> dp(number,start_value)
//assign(b.begin(),b.end())   [)
//clear back front pop_back push_back
//erase(a.begin()+1,a.begin()+3)  [)
//insert(a.begin()+1,b.begin()+3,b.begin()+5)

//priority_queue<int,<int>,greater<int>> small heap
//priority_queue<int,<int>,less<int>> big heap  //mo ren
//top push empty size pop

//queue
//front() back() push() pop() empty()

//string
//assign(num,char)
//assign(string,start,number)
//append(num,char)
//append(string,start,number)
//substr(start,number)

//to_string(value)

//#include<map>
//Map<int,int>  x;
//.size() .begin() .clear()
//.count()返回指定元素出现的次数
//erase() find() insert() swap()
//X.insert(pair<int, int>(1, 2))
//erase  erase（iterator it);//通过一个条目对象删除
//       erase（iterator first，iterator last）//删除一个范围
//       erase(const Key&key);//通过关键字删除
//map中的swap不是一个容器中的元素交换，而是两个容器所有元素的交换
//find()   strMap.find(string("mchen")
//map<int,int>::iterator


//pair<int ,double> p1;
//p1.first = 1;
//p1.second = 2.5;

//list
// list<int> (number,val)
//assign back clear erase insert pop_back sort
//list_t::iterator iter;
//for(iter = List.begin(); iter != List.end() ;)
//list_t::iterator iter ;
//
//iter = std::find(List.begin(),List.end(), info);
//
//if(iter != List.end())

#include <stdio.h>
#include <iostream>
#include <stdlib.h>
#include <string>
#include <math.h>
#include <limits.h>
#include <stack>
#include <map>
#include <vector>
#include <algorithm>
#include <unordered_map>
#include <queue>
#include <list>


using namespace std;
#define MOD 1000000007;



int main(int argc, char* argv[]) {

    return 0;
}
*/
#include <process.h>
#include <windows.h>
#include <thread>
#include <mutex>
#include <pthread.h>